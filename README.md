Portfolio for Computer Science subjects.

## Deployment

- Development source: `src/`
- Build output: `dist/`
- GitHub Pages source: `gh-pages` branch, `/` root folder

If GitHub Pages is pointed at `main`, the root `index.html` will show a deployment notice instead of the live portfolio.
