{
  "name": "portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && cd dist && git init && git add -A && (git commit -m \"deploy\" || true) && git push -f https://github.com/iamjgr/mine.git HEAD:gh-pages"
  },
  "dependencies": {
    "gsap": "^3.12.5",
    "three": "^0.161.0"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
